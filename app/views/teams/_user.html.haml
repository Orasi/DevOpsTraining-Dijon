%tr
  %td.table-number
    = "#{counter + 1}."
  %td
    = link_to user.username, user_path(user.id)
  %td
    - if current_user.admin
      = link_to remove_team_user_path(id: team['id'], user_id: user.id), method: :delete do
        .pull-right.fa.fa-remove{style: 'color:red'}